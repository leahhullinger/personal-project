// FOLDER DASHBOARD
// route = '/folders (need another word other than folder)

// a container to display all of the user's folders, description maybe
// class, this.state = {folders:[]}
/*
methods
- ComponentDidMount: axios GET request for all user folders
- this.state.folders.map() to display each Folder.js

*/

import React from "react";

export default function FolderDash() {
  return <div>Folder Dash</div>;
}
